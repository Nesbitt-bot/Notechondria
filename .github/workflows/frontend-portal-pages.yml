name: frontend-portal-pages

on:
  push:
    branches: [ main, codex ]
    paths:
      - 'frontend/portal_app/**'
      - 'frontend/shared/**'
      - '.github/workflows/frontend-portal-pages.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: '3.27.0'

      - name: Enable Flutter web
        run: flutter config --enable-web

      - name: Flutter pub get (shared)
        working-directory: frontend/shared
        run: flutter pub get

      - name: Flutter pub get (portal)
        working-directory: frontend/portal_app
        run: flutter pub get

      - name: Flutter build web (portal)
        working-directory: frontend/portal_app
        run: flutter build web --release --base-href /portal/ || echo "Build failed, checking for errors"

      - name: Stage portal site
        run: |
          mkdir -p /tmp/pages/portal
          rsync -a frontend/portal_app/build/web/ /tmp/pages/portal/ || echo "Stage failed"

      - name: Deploy portal to gh-pages/portal
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: /tmp/pages
          keep_files: true
          allow_empty_commit: false
